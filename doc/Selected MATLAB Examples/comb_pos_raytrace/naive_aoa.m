%% Naive DoA Function
% https://tns.thss.tsinghua.edu.cn/wst/docs/features/
function [aoa_mat] = naive_aoa(csi_data, antenna_loc, est_rco)
    % naive_aoa
    % Input:
    %   - csi_data is the CSI used for angel estimation [T S A E]
    %   - antenna_loc is the antenna location arrangement with the first
    %       antenna as a reference [3 A]
    %   - est_rco is the estimated radio chain offset [A 1]
    % Output:
    %   - aoa_mat is the angle estimation result [3 T]
    
end